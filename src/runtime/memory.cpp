// =============================================================================
// Ikaruga - Memory global
//
// ikaruga::g_mem aliases gcrecomp::g_mem (see include/ikaruga/runtime.h).
// No definition needed here — gcrecomp_runtime defines the single instance.
// This file is kept so the build target keeps a stable layout.
// =============================================================================

#include "ikaruga/runtime.h"
